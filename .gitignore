# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Excel temporários
~$*

# Runtime / local
users.db
server.log
server.err

# OS
.DS_Store
Thumbs.db

# Dev local
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml
.claude/
screenshots/
tools/*.csv
tools/*.json

# Backups (P7) — nunca subir para o repo. Use armazenamento externo se quiser
# guardar histórico (ver SYSTEM_DOC.md, seção Backup e Restore).
backups/
*.tar.gz

# Dados (não vão pro repo; produção usa o Volume do Railway)
dados/operacional/*.xlsx
dados/operacional/*.zip
dados/financeiro/*.xlsx
dados/financeiro/*.zip
!dados/**/.gitkeep
